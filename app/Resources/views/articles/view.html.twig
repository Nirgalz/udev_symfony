{% extends 'base.html.twig' %}

{% block body %}

    <table class="table">

        <thead>
        <tr>
            <td>Title</td>
            <td>Content</td>
            <td>Date</td>
            <td>Actions</td>

        </tr>

        </thead>

            <tr>

                <td>  {{ article.title }} </td>
                <td>  {{ article.content }} </td>
                <td>  {{ article.dateWritten|date("m/d/Y") }} </td>
                <td><a href="{{ path('article_view', {'id' : article.id}) }}"><span
                                class="glyphicon glyphicon-eye-open"></span></a>

                    <a href="{{ path('article_edit', {'id' : article.id}) }}}"><span
                                class="glyphicon glyphicon-edit"></span></a>
                    <a href="{{ path('article_delete', {'id' : article.id}) }}"><span
                                class="glyphicon glyphicon-remove"></span></a></td>


            </tr>


    </table>



{% endblock %}
